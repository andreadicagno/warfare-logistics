pre-commit:
  parallel: true
  commands:
    biome-check:
      glob: "*.{ts,tsx,js,jsx,json}"
      run: bunx biome check --no-errors-on-unmatched --changed --since=HEAD {staged_files}
    typecheck:
      run: bun run typecheck
